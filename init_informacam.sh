OLD_DIR=`pwd`
GPG_PASSWORD=$1
MASTER_PASSWORD=$2

# create a gpg key with this password

# append password to config.yaml

# append fingerprint to ictd

# save forms, ictd, gpg public key to local_remote

# encrypt config.yaml to password with iv and salt